export enum SERVICE_CONFIGS {
  NAME = 'service.name',
  DESCRIPTION = 'service.description',
  VERSION = 'service.version',
  BASE_URL = 'service.baseUrl',
}

export enum DOCUMENT_CONFIGS {
  BASE_URL = 'document.baseUrl',
  USERNAME = 'document.username',
  PASSWORD = 'document.password',
}

export enum SERVER_CONFIGS {
  HOST = 'server.host',
  PORT = 'server.port',
}

export enum DISTRIBUTION_CONFIGS {
  QUANTITY = 'distribution.quantity',
  SYMBOLS = 'distribution.symbols',
  RULES = 'distribution.rules',
}
